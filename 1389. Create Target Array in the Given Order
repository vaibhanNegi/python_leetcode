class Solution:
    def createTargetArray(self, nums: List[int], index: List[int]) -> List[int]:
        tr=[]
        i=0
        for i in range(len(index)):
            tr.insert(index[i],nums[i])
        return tr
        
