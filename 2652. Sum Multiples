class Solution:
    def sumOfMultiples(self, n: int) -> int:
        sum=0
        i=0
        while i<=n:
              if i%3==0 or i%5==0 or i%7==0:
                 sum+=i
              i+=1
        return sum
